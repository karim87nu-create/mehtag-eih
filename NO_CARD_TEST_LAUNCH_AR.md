# تشغيل تجريبي بدون كارت

اخترت Render للاختبار الأول لأنه يدعم Web Service مجاني وTLS/HTTPS مُدار.
الحزمة تستخدم:
- Web Service: Free
- PostgreSQL: Free
- لا Persistent Disk مدفوع
- DATABASE_URL يأتي تلقائيًا من قاعدة البيانات

## مهم
Render المجاني مناسب للاختبار وليس Production:
- الخدمة قد تنام بعد 15 دقيقة من عدم الاستخدام.
- أول طلب بعدها قد يحتاج قرابة دقيقة للإقلاع.
- قاعدة Postgres المجانية تنتهي بعد 30 يومًا.
لذلك الهدف هنا: أول 20 مستخدم وإثبات الحلقة، وليس التشغيل التجاري الدائم.

## ما أستطيع إقفاله بدون دخول حسابك
الكود وملف render.yaml جاهزان.
إنشاء خدمة Render نفسها يحتاج حسابًا خارجيًا باسمك/موافقتك، ولا توجد أداة Render متصلة بالمحادثة تسمح لي بإنشائها نيابة عنك.

## بعد وجود Repository
Render يقرأ render.yaml وينشئ الويب + قاعدة البيانات.
GitHub Actions الموجود في الحزمة يبني APK كـArtifact بدون Android Studio محلي.
